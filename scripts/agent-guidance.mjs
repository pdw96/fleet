// 세션 시작 시 에이전트 컨텍스트에 주입하는 규율 안내 — **호스트 중립 단일 출처**.
//
// ## 왜 별도 모듈인가
// 같은 문장을 호스트마다 복사하면 한쪽만 고쳐져 드리프트한다. 주입 **경로**는 호스트마다 다르지만
// (Claude Code = stdout 원문 · Gemini CLI = `hookSpecificOutput.additionalContext` JSON)
// 주입 **내용**은 하나여야 한다. 각 훅은 이 상수를 읽어 자기 호스트의 형식으로만 감싼다.
// 두 호스트가 같은 바이트를 내보내는지는 `scripts/session-start-hook.test.ts` 가 핀한다.
//
// ## 왜 포인터만 담는가
// 규율 본문(`AGENTS.md` 392줄)을 여기 복사하면 이중 권위가 되어 반드시 어긋난다
// (#280 이 동기화한 지 20분 만에 #279 가 무효화된 선례). 게다가 이 문자열은 **매 세션 로드**되므로
// 비대화가 곧 비용이다(ADR-0002 가 #27 본문에서 얻은 교훈). 그래서 「어디를 봐라」만 남긴다.
//
// ## 이것은 강제가 아니다
// 강제는 `npm run verify`·master ruleset·머지 게이트 훅이 한다. 이 안내가 하는 일은 그 게이트들에
// 닿기 전에 규율을 읽을 확률을 올리는 것뿐이다.
export const AGENT_GUIDANCE = [
  '[Fleet] 이 레포의 작업 규율은 AGENTS.md 에 있다 — 코드·문서를 바꾸기 전에 읽어라.',
  '        CLAUDE.md·GEMINI.md 는 그 파일을 가리키는 포인터일 뿐 규율 본문이 아니다.',
  '[Fleet] src/ 를 통째로 뒤지기 전에 brain.md(자동 생성 구조 지도)를 먼저 읽어 토큰을 아껴라.',
  '[Fleet] 변경 후 `npm run verify` green 이 머지 조건이다(로컬 == CI).',
].join('\n')
